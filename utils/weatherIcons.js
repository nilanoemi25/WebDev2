

/*export const WeatherIcons = {

    getWeatherCode(station){
        let weathercode = null; 
        if(station.reports.length > 0){
          weathercode = report.code;    
        }

        return weathercode; 
    }

} */